class testi {
    int ab = "abcde";
    char c = 'c';

    int a = 1000;
    int cd = (a + b);
    int ab = ab + cd + de;
    int aa() {
	    int bb = bbb;
	    aa  = aa+bb-cc-dd;
	    if ( id+bb+cc-dd ) {
		    if ( aaa ) {
                print(abc&&cbd,abc||cbd!=abc);
            }
            else {
                ab:while("abcd" || cd+!cd+(int)ab)
                    read(ab[ab],cd);
            }
}
	    aa = ccc ; 
    }
}
